# C5C6 — verdict: CONFIRMED (M-C5 F1 + numbered lists) / DOWNGRADED (M-C5 F2 parens+mailto, F3 tables; M-C6 email `&`)
- Why. F1 is real end-to-end and observed in production output, not just in a fixture: I rendered both real July flagship reports (`out/local-4837f6e3`, `out/local-aa4b3edf`, gitignored) through the production `buildReportHtml` (script `scratchpad/c5c6/render.mts`, same call shape as `apps/worker/src/pdf.ts:53-60`) and got 5 and 2 prose hrefs with `&amp;amp;` (`report-html.ts:123-124`: `esc(s)` then `esc(u)` on the captured, already-escaped URL) — one distinct URL per report (`miamidadematters…?indicatorId=393&localeTypeId=2`, `yelp.com/search?cflt=laundromat&find_loc=…`) out of 182/195 markdown links (~1–3%), while the SAME URL in the Sources list (`sourceListHtml`, single `esc`) is correct — the PDF disagrees with itself. I then printed the HTML with local Chrome via puppeteer-core with the worker's exact `page.pdf` options: the PDF DOES carry clickable link annotations (1,114 `/URI`, 626 `/Annot`), and 8 of them are `…indicatorId=393&amp;localeTypeId=2` — the click sends a parameter named `amp;localeTypeId` (for the yelp case `find_loc` is lost → search without location). Numbered lists: the real corpus has them (4 in one report, 1 in the other); the two single-newline ones (`Esto implicaría:\n1. …`, `financing_options`) render as ONE paragraph "… 1.  Comprar … 2.  Deshacerse …" (verified in the rendered HTML); blank-line-separated ones survive as numbered paragraphs (legible). NOT observed in either real report: a URL with `)`, any `mailto:`, any pipe table (0/377 links, 0 tables) — mechanism reproduced but unobserved, so those halves are P2. M-C6: `templates.ts:183` strips `&`; the headline is written by the flash model from buyer params (`jobs/headline.ts:36-40`, `industry` is free text ≤120 chars, `florida-business-for-sale.ts:410`), so "Bed & Breakfast"/"Bar & Grill" is an ordinary echo; HTML body reads "Bed  Breakfast" while subject and text part keep `&` (verified) — visible in every mail client, cosmetic.
- Severity: **P1** for F1 (deterministic, buyer artifact, observed in every real report that cites a multi-param URL; Sources entry stays correct so the buyer has a working copy elsewhere in the same PDF); **P1-low** for single-newline numbered lists (structure silently lost, observed 2× in one real report); **P2** for parenthesised URLs, `mailto:` in the PDF, GFM tables, and the email `&`. Damage statement: "In the PDF the buyer keeps, every prose citation of a URL with two or more query parameters is emitted as `&amp;amp;` and the PDF's link annotation carries `&amp;` — the click sends `amp;param`; and a numbered list attached to a prose line prints as one run-on paragraph; the ready email shows the headline without its `&` in the HTML body only."
- Fix: F1 narrowest = replace `esc(u)` with `u` at `report-html.ts:124` (u is already-escaped text; safe in an attribute) — one token, no honest cost, no existing test reds; tokenise-before-escape is fine but bigger. Parens: `(?:[^\s()]|\([^\s()]*\))+` — checked against the real `…store)) en maquinaria` case, still correct. `mailto:` in the PDF regex — agree (must not double-escape either). Numbered lists: the finder's `lines.every(/^\s*\d+[.)]\s+/)` `<ol>` branch does NOT fix the real shape (first line is prose) — split the block at the first list line (prose `<p>` + `<ol>`); same gap exists for `-` bullets after a prose line. Tables: 0 in the real corpus — cheapest is the directive word ("no tables"), not a renderer. Email: `esc()` the title/notice into HTML instead of stripping (keep raw in subject/text) — `email-language.test.ts` and `apps/worker/test/run.test.ts:138` stay green (they assert `toContain(notice)` with `&`-free copy).
- Test quality (all in `packages/core/test/red-team/c-legit.test.ts`): F1 `:52` asserts content (`href` toBe url), fails for the stated reason (observed `&amp;ref=list`); mutation that greens it: `esc(u)`→`u`. Paren `:64` content, fails as stated (`…(city` cut); greens with balanced-paren regex. mailto `:72` content (`hrefs` toContain), fails as stated (literal brackets); greens by adding `mailto:` to the regex. ol `:92` content (`<ol><li>Request`), fails as stated; greens with an `<ol>` branch — but that branch alone leaves the real mixed-block shape flattened (my `refute-c5c6.test.ts` pins it). table `:99` content (`<table`), fails as stated. F5 `:198` content (`Bed &amp; Breakfast` in html + `&` in subject), fails as stated; greens by escaping. All `it.fails` flip red as `it` for the claimed reason (ran with `-t "PDF|email"`: 5 red-as-expected, guards green). New: `packages/core/test/red-team/refute-c5c6.test.ts` (5 tests, green as written; 2 `it.fails` red today: prose-vs-Sources disagreement on the real URL, and the real single-newline list shape; 3 pins: single-param URL unaffected, blank-line numbered items survive as `<p>1.  …`, email keeps `&` in subject/text and loses it only in html).
- Worse (verified): the PDF is NOT inert text — Chrome print emits `/URI` link annotations, so the wrong URL is what the buyer's click actually opens (8 annotations with `&amp;` in the real report's PDF); and the finder's proposed `<ol>` fix misses the shape the model actually writes.
