# B2 — verdict: CONFIRMED (honest core, on the real traces) — attack halves DOWNGRADED to P2

- Why. I read both July traces myself (`out/local-4837f6e3`, `out/local-aa4b3edf`, gemini-2.5-flash loop, budgets = today's template, depth standard — the only scale at which the arithmetic lands) with `packages/core/test/red-team/refute-B2.test.ts` (6 tests, green): tool notes are one per loop iteration (292 real tool calls, min gap between two notes 1,147 ms while the emit overhead — last note → "Writing" on a bound exit — is 1 ms: no batched plan+search in a single Gemini response, ever), and three agent-runs sit EXACTLY at `2·budget+6` (`gather.ts:180`): aa4b3edf **deep-dive-refiner 26/26 = 22 plans + 4 cached + 0 turns** (`P c P c P c P c` then 18 plans in a row, agent bill $0.383, 572k input tokens, zero evidence bought), aa4b3edf **risk-analyst 16/16 = 16 plans, 0 turns** ($0.232), and 4837f6e3 **deal-scout 54/54 = 24 plans + 24 paid + 6 cached, 24/24 spent** — replayed literally through today's `gather` it ends `stop:'stalled'`, `gatherCompleted=false` (refute-B2 §2). Two things the finders under-stated make the 0-turn loops WORSE than "the model re-plans": `forceTools: turnsUsed === 0` (`gather.ts:219`) is Gemini `mode: ANY` (`gemini-vertex.ts:64-65`) for EVERY iteration until a turn is spent, so a loop that decides it has nothing to search cannot answer without a tool — the bound is its only exit and the nudge branch (`gather.ts:234-243`) is dead code under Gemini; and no note/field records the exit (`GatherStop` is dropped at `research-engine.ts:747-755`). What does NOT stand: (i) B-attack's "400 `update_plan` in ONE turn": at `gatherMaxOutputTokens=4096` (`config.ts:313`) a minimal call is ~18-27 tokens → ≤ ~150-227 calls per response, realistically 10-20 (B-attack's own aside); evicting the "Writing" note needs ≥299 notes from ONE agent, i.e. ≥6 plans/turn on every one of the deal-scout's 54 turns (22/turn on the F2 fixture's budget 4) — reachable only as a sustained multi-turn obedience, and what it costs is admin notes + Firestore `setProgress` writes (`run-job.ts:304-317`, ~$0.002/1,000) — hygiene. (ii) B-attack F5's "528k chars on 2 turns" is SMALLER than D-legit's honest re-planning deal-scout at the same 54-iteration bound (1,363k); the growth is the loop's, not the attack's; the attack's damage is evidence denial (24 turns → 2), report completes. (iii) B-legit's "doing→done stalls on every comprehensive agent" is a hypothetical persona: the real model is the `revise` persona (plans ≈ turns+1; max 2 consecutive plans across all 18 honest agent-runs) — the real honest stall is the cross-check one (deal-scout run 1). (iv) D-legit's "8/10 producers hit the bound in the mock" is a fixture artifact: the fake web has 9 pages, so its diligent agents made ~42 cached (free) re-reads per job (154−92−20 in its own plan-once numbers) where the two real jobs made 12 and 5; the real rate is 1 honest bound-hit (at 100 % budget) + 2 pathological plan-loops in 20 producer-runs.
- Severity: **P1** (money + buyer content, honest path, evidenced in production traces). Damage statement: *In a real July comprehensive job, two of ten producers never searched — Gemini forced into a tool call at 0 turns re-planned to the 2·budget+6 bound (26 and 16 iterations, ≈$0.6 of agent spend, sections written from no new research), and the honest deal-scout that spent 24/24 ended at the bound and is classed `stalled`, so one flaky write re-buys the job's most expensive loop ($1.19 agent); none of it is recorded; the attacker's plan-spam is a cheaper copy of the same loop and its note/progress flood is hygiene (P2).*
- Fix: **agree** with `stalled && turnsUsed >= maxTurns → 'budget'` (real corroboration: 4837f6e3 deal-scout) and with recording `GatherStop` in a note + trace field. **Narrower** than a consecutive-FREE-call breaker ≥4: a consecutive-PLAN breaker (≥3-4; real honest max is 2) — the honest 4837f6e3 deep-dive-refiner is `P c P c P F P` (5 free calls in a row BEFORE its only paid fetch), so the ≥4 free-call breaker cuts a real honest agent before its one turn (refute-B2 §1/§2); the plan-only breaker leaves it alone and ends both real plan-loops at call 3-4. **Hidden costs the finders missed:** (a) a "stop planning: search or write" tool result is unactionable under `forceTools`/mode ANY — the model cannot answer without a tool while `turnsUsed === 0`; the breaker must break the loop itself (or lift `forceTools` after the first plan), not ask; (b) "drop superseded `update_plan` results / cap plan history" must STUB the args/results (as `PAGE_STUB` does), not delete messages — Gemini rejects a `functionCall` part without its matching `functionResponse`; (c) the F5-style `P c P c` alternation is not caught by a plan-only breaker — cap same-URL cached re-reads (F5 refetches ONE url every turn) rather than charging cached reads (the honest refiner's whole job is cached reads). Coalescing plan notes to 1/turn costs nothing honest; drop-oldest trades the head ("Researching", queries) for the tail — keep first N + last M instead.
- Test quality: `b-attack F2 it.fails` — asserts content ("Writing" survives, <50 plan progress lines); fails for the stated reason under the mock, but the shape (400 calls in one response, budget 4) is unreachable at the 4,096-token output cap; at ≤227 calls "Writing" survives on that fixture. Control pins <5 plan lines (mutation: emit per plan call still passes — it is the flood that reds it). `b-attack F5` — asserts loop>40 / turns≤3 / last>3×first (content), but does not distinguish attack from the honest re-planner at the same bound; a breaker reds it (finder pin, expected). `b-legit 1 · three personas` — content (revise always `budget`+reusable; deal-scout revise+cross-check 24/24 `stalled` = the REAL 4837f6e3 sequence; "max free run 3" is persona-only — real honest max is 5); `headroom is 4` — revert-verified by the finder (`+6→+5`), and reds under the `stalled→budget` fix (5 rereads at full budget → `budget`). `b-legit 1 · doing→done it.fails` / `2 · reused after failed write it.fails` — fail for the stated reason (doing→done = 2B+1 iterations), but the persona is not the real model's; same mechanism reaches production via the cross-check stall. `d-legit 2 · 8 of 10 hit maxIterations` — asserts 8/79 (content) but the number is a 9-page-fixture artifact (cached hits); the PROPERTY `it.fails` is right in kind. `d-legit 3 · STALLED at 26` / `18 turns billed` — content; reds under the `stalled→budget` fix (expected). No test outside `red-team/` pins `+ 6`, `Plan updated`, or `stalled` at full budget (`retry-waste.test.ts:205-209` pins `gatherCompleted` on literals — unaffected). refute-B2 §2 pins the deal-scout replay as `stalled` (mutation that reds it: the two-line fix — the pin is meant to flip).
- Worse than reported (verified): `forceTools` (mode ANY) at `turnsUsed === 0` makes the iteration bound the ONLY exit for a producer that judges it has nothing to search — the two real plan-loops are the loop's design, not only the model's habit, and the nudge path never runs under Gemini; the affected agent in run aa4b3edf was the deep-dive-refiner, i.e. the "pro pass" section (`deep_dives`) was written from zero new research at $0.38, invisible in the trace.
