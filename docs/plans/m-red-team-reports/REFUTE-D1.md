# D1 — verdict: DOWNGRADED

- **Why.** The mechanism is real and I re-verified every link: `research = { done: false }` is a per-dispatch local (`research-engine.ts:439`) never written to `Checkpoint` (`:174-197` has `doneAgentIds` but no gathered-set; `snapshot()` `:349` writes none), so every re-dispatch re-runs the loop; the attempt loop is `config.workflow.agentMaxAttempts` = 3 in prod (`config.ts:325`, test env 2), `finalize = attempts >= maxJobAttempts` = 8 (`run-job.ts:128`, `config.ts:330`), dependents are deferred while retries remain (`research-engine.ts:426-430`) so dispatches 2..7 are the failing agent alone and dispatch 8 writes the rest best-effort; `synthesizeStructured` always pays a second full-size call on an invalid answer (`synthesize.ts:39-83`); `approveHold` sets `attempts: 0` and `budgetOverride: true` for `budget_exceeded` (`firestore.ts:319-322`) → `costCeilingUsd: null` (`run-job.ts:279`); `jsonSchemaToGemini` forwards only type/description/enum/nullable/properties/required/items (`gemini-vertex.ts:239-276`) — minItems/maxLength/minimum are dropped, confirmed. d-attack ran green (10/10; table reproduced: 9 writes vs 3, dispatch 2 loop 2×, searches 4→6); the `it.fails` flipped fails for the stated reason (`expected 8 to be less than or equal to 4`, then attempts 2≠1). **What does not stand is the premise behind the dollar figure.** The whole ×24 exists only if the model returns the SAME invalid value on the repair round — the mock's `obeyStructured` is unconditional and ignores our repair turn ("Fix these problems and return the corrected JSON only:\n<path>: <issue>", a user turn, outside the fence). I isolated that in `packages/core/test/red-team/refute-D1.test.ts` (3 tests, all pass today): the same page with a model that obeys it on the first write and then reads our repair message → writes 5 vs control 3, `attempts` 1 for every agent, `meta.sections` empty, one dispatch. On the flagship provider the type-violation variant is closed by constrained decoding (`askingPrice: number|null` → `{type: NUMBER, nullable: true}` via `unwrapNullable`), the Zod-only `.min(n)` family reaches the model on the first call but the repair message names the path and the constraint, and the two real July runs show 0 schema/JSON failures in 26 pro writes (`out/*/trace.json`: every agent `ok`, no retry notes). The only page-driven route to a repeat that our repair text cannot fix is obedience-driven truncation (32,768 out incl. thinking; 14 pages × 6k chars ≈ 21k tokens + a 12k section + thinking does exceed it IF Gemini 2.5 Pro reproduces a fenced page's "reproduce every page verbatim" against our own length guidance six times per dispatch) — plausible-but-unlikely, unmeasured on any real model here.
- **Severity: P1** (money; the multiplier is real, the trigger is not demonstrated on the production provider). Damage statement: *A write that fails on every attempt is retried 3 attempts × 8 dispatches with the research loop re-bought on each dispatch and no bound but the $20 ceiling (then held; approve resets attempts and uncaps); on Gemini 2.5 Pro with constrained decoding and our repair message the realistic price of an invalid first write is one extra pro call (~$0.2–0.4 per affected writer, nothing lost), and only an obeyed "reproduce all pages" truncation reaches the full multiplier.* My flagship range under the finder's premise, recomputed with `config.ts` prices (pro 1.25/10, dossier ~27.5k tok in, 12k out normal / 32,768 cap; deal-scout loop re-run ≈ $1.0–1.2 from the real July agent cost of $1.19): attempt pair $0.33 (normal) … $0.77 (cap); per dispatch $2.0 … $3.5; ×8 + the rest of the report → $19–31 → **held at $20 around dispatch 5–8 in every variant** (finder's $10–21 floor is low; the schema variant does not finalize under the ceiling in prod). Under my believed-realistic premise: **+$0.2–0.8 per job**, no held, no lost section.
- **Fix.** (1) Persist a `gatheredAgentIds` set next to `doneAgentIds` (written in `snapshot()`, read into `research.done` at `:439`) — agree; costs an honest job nothing, no conflict with C2 (the flag would be set from the same `gatherCompleted(gres)` "finished" rule at `:755`, and the checkpoint is only saved after the attempt loop, so a crash mid-agent still re-runs). (2) "Two identical Zod issue strings → degrade" is too blunt AND too narrow: too blunt because Zod messages carry no value (`Too big: expected string to have <=80 characters` is identical for 82 and 95 chars — an honest near-miss on the second try loses the section that attempt 2 might save); too narrow because JSON-parse errors carry a position (`Unterminated string in JSON at position N`, verified) so the truncation variant escapes string equality. Narrower: keep the in-dispatch attempts, persist a per-agent failure signature (issue path+code, JSON-parse kind without position) in the checkpoint and stop RE-DISPATCH retries for an agent whose signature repeats (kills the ×8, keeps the ×3), and forward `minItems/maxItems/minimum/maximum` in `jsonSchemaToGemini` — `@google/genai` 1.52's `Schema` has them and `responseJsonSchema` documents them as supported (`genai.d.ts:4558`), which closes 14 of the ~17 Zod-only Florida constraints at the decoder. (3) `approveHold` not resetting attempts is moot once (2) prevents the deterministic case reaching the ceiling, and it has a hidden cost the finder missed: an approved legit job held at dispatch 8 would `finalize` immediately with degraded sections (the comment at `firestore.ts:296` is right). Also the "admin is not informed" claim in F2 is soft — the trace with per-agent `error`/`attempts` is uploaded and attached to the hold (`run-job.ts:422,437`).
- **Test quality.** `D1 > control` — content (3 writes, attempts 1). `D1 > measured 9` — content, fails for the stated reason (asserts `error` matches `askingPrice`, sections `lost`, loop == control); mutation: `ctx.research.done = false` at `:755` reds the loop-equality; `attempt < 2` → `< 1` in `synthesize.ts:39` reds the 9. `D1 > NEXT dispatch` — content; the finder's fix (1) reds `second.m.loop === 2×afterFirst` (becomes 1×) — good pin. `it.fails DEFECT` — fails for the stated reason (verified: 8 > 4); note it also flips green on a pure "AGENT_MAX_ATTEMPTS=1" number fix, not only on the identical-issue rule. `truncated` — content (9 writes, error regex); the 32,768 pin is a config-shape assertion. `jsonSchemaToGemini drops` — content, verified in source; forwarding minItems reds it (intended tripwire). `D2 40 loop calls` — content, pins B's 20 × attempts. `D3 held` — content (held, budgetExceeded, progress text, uncapped resume ≥20 loop/≥4 writes); a fix that stops re-attempting after a repeated signature reds it. d-legit § 3 (4 tests) — content, all green (0.4 s); the 18-vs-10 stalled table reproduced. All cited tests measure the mock's premise faithfully; none measures whether a real model repeats after the repair message — that is the load-bearing unknown, and it is stated as such only in the finder's note (c).
- **Worse (verified):** none. Checked and not worse: the queue's `--max-attempts=12 --max-retry-duration=10800s` (`infra/setup-gcp.sh:109`) does not cut the 8-dispatch chain short (both limits must be reached), so the ×8 is real if the trigger is; the trace attached to the hold does let an admin see the repeated failure before approving.
